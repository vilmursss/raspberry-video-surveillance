# Where the project repo is mounted in docker container
VOLUME_WORKDIR="/workdir"

# Where the image (.wic) is decompressed after build
IMAGE_DECOMPRESSED_DIR="/mnt/tmp"

# Yocto workdir in host machine
YOCTO_WORKDIR="/mnt/lacie/yocto-raspberry"

# Yocto build directory. Note: DO NOT MODIFY THIS VARIABLE.
# It is eventually required by BitBake through TOPDIR variable.
YOCTO_BUILD_DIR="${YOCTO_WORKDIR}/build"
